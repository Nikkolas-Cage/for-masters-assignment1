import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  // Add your custom theme configuration here
  // Example: custom fonts, colors, etc.
  // fonts: {
  //   heading: 'var(--font-rubik)',
  //   body: 'var(--font-rubik)',
  // },
});
